{% extends 'admin_base.html.twig' %}
{% block title %}Editer FicheMatch{% endblock %}

{% block body %}
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">Fiches Matchs</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="{{ path('fiche_match_index')}}">Fiches Matchs</a></li>
                        <li class="breadcrumb-item active">Edition</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="card card-info mb-5">
            <div class="card-header">
                <h1>Modifier la fiche du match</h1>
            </div>
            {{ include('fiche_match/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
{% endblock %}
