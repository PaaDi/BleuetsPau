{% extends 'base.html.twig' %}

{% block title %}FicheMatch{% endblock %}

{% block body %}
    <h1 style="margin-top: 120px;">FicheMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_match.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ fiche_match.DateDebut ? fiche_match.DateDebut|date('d-m-Y H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fiche_match.Nom }}</td>
            </tr>
            <tr>
                <th>Stade</th>
                <td>{{ fiche_match.Stade }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_match_index') }}">back to list</a>

    <a href="{{ path('fiche_match_edit', {'id': fiche_match.id}) }}">edit</a>

    {{ include('fiche_match/_delete_form.html.twig') }}
{% endblock %}
